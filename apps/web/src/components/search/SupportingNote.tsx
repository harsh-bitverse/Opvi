import React from 'react';

export function SupportingNote() {
  return (
    <p
      style={{
        marginTop: '1.25rem',
        marginBottom: '2rem',
        fontSize: '0.875rem',
        color: 'var(--text-secondary)',
        textAlign: 'center',
        fontWeight: 400,
        letterSpacing: '-0.005em',
        lineHeight: 1.5,
      }}
    >
      Searching works instantly without signing in. Sign in to unlock personalized recommendations &amp; alerts.
    </p>
  );
}
